# Legacy benchmark report retired

The earlier Flow 1 microkernel and Flow 2 structural numbers are not valid
production-GEM/upstream speedup evidence. The current generated report is
[`results/performance_report.md`](results/performance_report.md).

Reproduce it with:

```shell
python3 benchmark/scripts/run_benchmarks.py --repetitions 7
```
